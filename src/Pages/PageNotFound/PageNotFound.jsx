import React from "react";

const PageNotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        marginTop: "6rem",
        fontSize: "2rem",
        textAlign: "center",
      }}
    >
      PageNotFound Error # 404
    </div>
  );
};

export default PageNotFound;
